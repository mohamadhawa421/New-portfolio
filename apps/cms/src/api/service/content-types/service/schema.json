{
  "kind": "collectionType",
  "collectionName": "services",
  "info": {
    "singularName": "service",
    "pluralName": "services",
    "displayName": "Service",
    "description": "The \"Five ways to work together\" list. Also the Work page's filter chips and the enquiry form's service options. Each one preselects itself in the form."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "required": true
    },
    "ctaLabel": {
      "type": "string",
      "default": "Start here"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "Lowest first. Drives the 01 / 02 / 03 numbering."
    }
  }
}
